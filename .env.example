# Abstrabit RAG Inspector Configuration
# Demo Mode requires zero external API keys. Set to true for 100% deterministic local execution.
DEMO_MODE=true

# Optional: Set OpenAI-compatible API key and endpoint for live LLM AI investigation
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
